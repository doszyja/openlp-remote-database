/**
 * @openlp/shared - Shared types and DTOs
 *
 * This package contains shared TypeScript types, interfaces, and DTOs
 * used across the monorepo (backend, frontend, sync tool).
 */

export * from './types';
export * from './dto';
